# ============================================================================
#  Postfix master.cf  -  TatvaOS Mail local development
# ============================================================================
#  service      type  private unpriv  chroot  wakeup  maxproc  command + args
#                     (yes)   (yes)   (no)    (never) (100)
# ============================================================================

# ----------------------------------------------------------------------------
# Inbound SMTP :25  -  "the internet" in local dev (host port 2525)
#
#  Inherits smtpd_recipient_restrictions from main.cf, which deliberately does
#  NOT begin with permit_mynetworks. This port must never trust its client -
#  it accepts mail addressed to us and refuses everything else.
# ----------------------------------------------------------------------------
smtp      inet  n       -       n       -       -       smtpd
    -o smtpd_client_restrictions=

# ----------------------------------------------------------------------------
# Submission :587  -  authenticated client send (host port 5870)
#
#  THIS is where client trust belongs, and only here. Relaying to external
#  domains is the entire purpose of a submission port, so permit_mynetworks
#  is correct at this position and dangerous on port 25.
#
#  Production requires TLS and SASL here. Local dev accepts unauthenticated
#  submission from mynetworks so swaks and the API can send without a
#  certificate. These three -o lines must NOT be copied to production.
# ----------------------------------------------------------------------------
#  PARAMETERISED since 28 Aug 2026. The three dev-only "-o" lines that the
#  comment above warned must never reach production WERE production — one
#  master.cf, no per-environment split, so the warning was a wish. The values
#  now come from main.cf parameters: local defaults stay exactly as the old
#  literals, and the entrypoint overwrites them with the authenticated-TLS
#  posture when TATVAOS_ENV is not local. The file can no longer disagree
#  with the environment it runs in.
submission inet n       -       n       -       -       smtpd
    -o syslog_name=postfix/submission
    -o smtpd_tls_security_level=$submission_tls_security_level
    -o smtpd_sasl_auth_enable=$submission_sasl_auth_enable
    -o smtpd_sasl_type=dovecot
    -o smtpd_sasl_path=inet:dovecot:12345
    -o smtpd_sasl_security_options=noanonymous
    -o smtpd_client_restrictions=$submission_client_restrictions
    -o smtpd_sender_restrictions=$submission_sender_restrictions
    -o smtpd_recipient_restrictions=$submission_recipient_restrictions

# ----------------------------------------------------------------------------
# API submission :10587  -  our own application, inside the compose network
#
#  NOT published to the host in any compose file, and must never be. It is
#  reachable only from containers on mailnet, and that is the entire trust
#  argument: permit_mynetworks here means "a container on this box", because
#  nothing else can open the socket.
#
#  WHY IT EXISTS: 587 is the CUSTOMER port. Production rightly requires TLS
#  and a SASL password there (entrypoint.sh applies that posture), and the
#  API has neither - it sends on behalf of shared mailboxes that by design
#  have no password at all. The moment the authenticated posture landed on
#  587, every send through the API on production started failing: Postfix
#  answers MAIL FROM with 530 "Must issue a STARTTLS command first", MailKit
#  raises that as ServiceNotAuthenticatedException, and MailSender's
#  catch-all reported it as "The mail server could not be reached". Webmail
#  compose included. Found on 3 September by the send API, which was simply
#  the first caller anyone was watching.
#
#  Same sender gate as 587 (verified-domain outbound), same milters (DKIM
#  signs it), no TLS and no SASL because this hop never leaves the host.
# ----------------------------------------------------------------------------
10587     inet  n       -       n       -       -       smtpd
    -o syslog_name=postfix/api
    -o smtpd_tls_security_level=none
    -o smtpd_sasl_auth_enable=no
    -o smtpd_client_restrictions=permit_mynetworks,reject
    -o smtpd_sender_restrictions=$submission_sender_restrictions
    -o smtpd_recipient_restrictions=permit_mynetworks,reject_unauth_destination,reject

# ----------------------------------------------------------------------------
# Bounce intake transport  -  delivers a validated bounce to the API over LMTP
#
#  main.cf routes bounces.tatvaos.com here (transport_maps -> bounce-intake:).
#  Its OWN service, not the shared `lmtp` client, so bounce delivery carries
#  its own syslog_name (greppable on its own) and can be tuned without touching
#  how anything else relays over LMTP - the same "own failure mode, leave the
#  shared one alone" split the bounce policy gate uses in main.cf.
#
#  The nexthop is set in main.cf as [api]:10035. The [] disables MX lookup so
#  Postfix connects straight to the compose host `api`; the API's
#  BounceIntakeWorker is the LMTP server there. Nothing here is published to
#  the Docker host - only containers on mailnet reach that socket, which is the
#  whole trust argument, exactly as for the :10025 quota service.
# ----------------------------------------------------------------------------
bounce-intake unix  -       -       n       -       -       lmtp
    -o syslog_name=postfix/bounce-intake

# ----------------------------------------------------------------------------
# Standard internal services
# ----------------------------------------------------------------------------
pickup    unix  n       -       n       60      1       pickup
cleanup   unix  n       -       n       -       0       cleanup
qmgr      unix  n       -       n       300     1       qmgr
tlsmgr    unix  -       -       n       1000?   1       tlsmgr
rewrite   unix  -       -       n       -       -       trivial-rewrite
bounce    unix  -       -       n       -       0       bounce
defer     unix  -       -       n       -       0       bounce
trace     unix  -       -       n       -       0       bounce
verify    unix  -       -       n       -       1       verify
flush     unix  n       -       n       1000?   0       flush
proxymap  unix  -       -       n       -       -       proxymap
proxywrite unix -       -       n       -       1       proxymap
smtp      unix  -       -       n       -       -       smtp
relay     unix  -       -       n       -       -       smtp
showq     unix  n       -       n       -       -       showq
error     unix  -       -       n       -       -       error
retry     unix  -       -       n       -       -       error
discard   unix  -       -       n       -       -       discard
local     unix  -       n       n       -       -       local
virtual   unix  -       n       n       -       -       virtual
lmtp      unix  -       -       n       -       -       lmtp
anvil     unix  -       -       n       -       1       anvil
scache    unix  -       -       n       -       1       scache
postlog   unix-dgram n  -       n       -       1       postlogd
