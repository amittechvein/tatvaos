# Review — 30 August 2026

## What shipped

- Connect recording sharing (read side). Endpoints written, database schema drafted, landing when the draft gets Core's sign-off.
- Mail reading pane: Reply labelled and primary, destructive actions moved behind a divider. Compared against production screenshots and re-checked after deploy.
- Deploy lock: one deployer at a time, enforced with flock. Second deploy refused with the holder's name and connection. Proven against concurrent runs, kills, and drops.
- IMAP probe fixed. Was false-alarming on every deploy (got EOF before Dovecot greeting) and passing only against warm servers—the worst calibration a check can have. Now pipes LOGOUT and waits for the answer.
- Connect migrations renamed to true dates. Ten files wearing September labels were written in August, breaking any fresh database. Fixed and verified twice.
- House rules consolidated. Rule 11 ("every lane merges and deploys its own work") lived only in chat; now it's part of one authoritative file.

## What I sent back

Nothing. The recording-shares draft is in proposals/ marked unregistered, as intended. All merges and commits declare their module and pass the eleven house rules.

## Needs Amit

When should the recording-shares migration move from proposals/ into live, and be promoted to a date-prefixed name in local/postgres/init/? The read side is reviewed and the tables are documented; the gate is whether field testing can wait or whether it lands with the code.

## Quiet risks

- Six worktrees marked `prunable`—branches merged, local copies stale. Clean when convenient.
- The three future-dated migrations (20260911-*) are real files not yet reconciled with when they land. Verify the mail-app-passwords and core-ai-per-org ones have the right orderings before they replay.
