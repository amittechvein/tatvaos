# Review — 5 September 2026

Window: everything on `main` from yesterday 10:00 to the laptop's last fetch (20:49 last night — the VM cannot reach GitHub, so anything pushed after that is tomorrow's). Fifteen PRs merged. Three of yesterday's four send-backs are done and proven; one is not.

## What shipped

- Mail API keys are now honest: a key with no sender addresses refuses to send (it used to mean "send from anywhere"), the database rule that backs this actually refuses (I inserted an empty list in the VM — rejected), and the migration that broke every second deploy now runs three times clean. The sender picker on the API-keys page loads again.
- API sends go out one message per recipient, and the reply says per recipient what was accepted and what was refused, so a caller retries only the failures. Each message carries a return address that names its own log row, so a bounce days later can be matched — dormant until the bounce mailbox exists (that branch is next).
- Connect: a host can reopen an ended meeting and let a removed person back in (the 3 September misclick that needed a 5 a.m. database edit). In a Private meeting, sending a file now asks first — the red warning promised it, nothing did it.
- Connect picture-in-picture: phones held upright no longer crop to a band of eyes; your own tile mirrors like the stage.
- Mail settings are now tabs (Signature / Folders / Categories / Filters / Blocking); inbox layout switches from the inbox toolbar.
- People page shows Last sign-in as a column.
- Recovery email, stages 1–3: columns, a status endpoint, and set-plus-verify endpoints. **Nothing a user can touch yet** — see below.

## What I sent back

1. **Core (recovery email) — the verification email's button lands on a page that does not exist.** The link goes to `/verify-recovery-email`; the web app has no such page (reset-password has one; this copied the pattern but not the page). Nobody can complete verification, and there is no settings screen to enter a recovery address either. Build both before Stage 4 merges.
2. **Core (recovery email) — setting a recovery email must ask for the current password, notify the primary mailbox, and write an audit line.** Today it does none of the three. Chain it with the pending Stage 4 branch: someone who borrows a signed-in session adds their own address, clicks the link, later resets the password through it — full takeover, and the real owner is never told. The password-reset flow next to it audits; this must too.
3. **Core — nine commits yesterday are signed `Deploy <deploy@tatvaos.local>`.** That is the server's account, not a person. Either someone is writing code on the production server, or a worktree has the wrong identity. Tell me which, fix `git config user.name/email` there, and if it is the server: stop — code is written on laptops and deployed by the workflow.
4. **Core — still on `main` from yesterday: the 74 KB tarball under `Claude outputs/`, `tatvaos-bundle.bundle`, and `apps/mobile` with no owner.** Item 2 of yesterday's note; nothing moved. Remove the two binaries in one PR; the mobile app waits for an assignee.
5. **Amit — one file to delete in your folder, today.** `local/postgres/init/20260904-user-recovery.sql` is an untracked early draft (the real one is `…-user-recovery-email.sql`, merged). I ran it in the VM: it fails — it forces every user's phone to be non-empty and trips on the blank ones. If it is ever `git add`-ed, every deploy after that stops. In `PS C:\Users\amitd\Downloads\tatvaOS>` run `Remove-Item local\postgres\init\20260904-user-recovery.sql` — good looks like `git status` no longer listing it. (Your folder is also on branch `docs/hr-roadmap`, not `main`; the deploy sequence starts with `git checkout main`.)

## Needs Amit

Nothing new today. Yesterday's branch-protection question is still open — the nine `Deploy`-signed commits make the answer matter more, not less.

## Quiet risks

- The recovery-email feature is three of five stages in; stage 4 (reset via recovery email) is on a branch and reads correctly. It is only safe once item 2 above is done — I will not clear that branch before.
- The bounce return address is signed with a secret that will live in `infra/docker/.env`, which `backup.sh` copies into every backup. Same blast radius as every other secret; noted so nobody thinks it is different.
- Four Connect migrations still dated 11–12 September; the prep branch to fix them is a week old and unmerged. Unchanged.
- Yesterday's review is committed on `docs/hr-roadmap`, not on `main` — it needs the PR. Mine: this note is written into your folder and, like every day, not committed.
