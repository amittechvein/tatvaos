# Review — 13 September 2026

Written by the CTO at the end of the last Cowork session. From 14 September
every lane runs in its own Claude Code session, each in its own worktree under
`C:\Users\amitd\Downloads\tatvaos-one\`. This is the state they inherit.

---

## 1. Where things stand

| | State |
|---|---|
| Lanes | Mobile, Hire & People, Mail, Connect, Core — **all handed over and closed.** Handovers are in the CTO's possession; the durable parts are in `docs/onboarding/<lane>/WELCOME.md` and `docs/decisions/`. |
| `main` | `6f0ded8`. CI green. |
| Production | **Up, verified from outside (`HTTP 200`). Serving `6f0ded8`, the current `main`** — corrected 13 Sept, see §3. The line here originally said "the build from ~13:00 IST on 12 Sept, not the current main"; that was wrong. |
| Folders | All six checkouts moved to `tatvaos-one\`. Worktree pointers repaired and verified in both directions. |

## 2. Merged today

| PR | What |
|---|---|
| #94 | Rule 6b — a result is about a version — re-applied on current `main` (#75 is superseded and should be closed) |
| #95 | `.github/workflows/migrations.yml` — every init file built from nothing, then again. Ran on its own PR; path filter verified both firing and not firing |
| #96, #97 | `docs/decisions/0001` — the rejected `user-recovery.sql` migration, and its same-day correction |
| #98 | `core.entitlement_overrides` — Core's table, landed from a verified copy; **org revoke is an absolute veto** (ruling in the file header) |
| #99 | Mail and Connect onboarding welcomes; README corrected; **house rule 6c** |

## 3. The deploy that did not land — CORRECTED 13 Sept: it landed

**Correction, written by the developer on 13 September 2026 after measuring
on the box, with the CTO's own account of the error quoted below rather than
softened.** The section as first written said the deploy of `6f0ded8` did not
recreate `web` and that production served the previous build. Both claims were
wrong. The original text is kept underneath, struck through in substance rather
than deleted, because a review that silently rewrites itself is the failure
rule 6b names.

**What is actually true, measured 13 Sept between 10:06 and 10:20 UTC over SSH
as the deploy user:**

| Question | Command | Result |
|---|---|---|
| When were the app containers created | `docker ps -a --format` with created and status columns | `web`, `api`, `postfix`, `dovecot`, `opendkim` all created **2026-09-12 20:18:46–20:18:51 UTC** — thirty seconds after the deploy's pre-deploy backup at 20:18:17 |
| Does `web` run the image the deploy built | container `.Image` vs `docker image inspect tatvaos-web` `.Id` | identical (`sha256:03d54ebc…`), image built 20:18:04 UTC |
| Which commit is inside the container | `grep NEXT_PUBLIC_BUILD_SHA apps/web/server.js` in `tatvaos-web-1` | `6f0ded8` |
| Which commit do the public pages say | the `x-build` meta tag on core.tatvaos.com, its login page, and mail.tatvaos.com | `6f0ded8` on all three |
| Did the migration land | `\dt core.entitlement_overrides` | one row — the table exists |
| Any other deploy that night | one `backups/pre-deploy-*.sql` per `deploy.sh` run; `uptime -s`; shell history | exactly one run, 20:18 UTC; no reboot since 21 Aug; no hand deploy |

**Why the check went red anyway.** `apps/web/Dockerfile` set `BUILD_SHA` in
the *build* stage only. Environment does not cross a `FROM` boundary, so the
runtime image has never carried the variable — the count of `BUILD_SHA=` lines
in both the running container's environment and the freshly built image's is
**zero**. The "Confirming the running build" step reads exactly that
environment, so it took its *cannot read* branch, which means "I could not
look", not "the containers were not replaced". This was the check's **first
ever run**: it merged in #84 at 18:29 UTC on 12 Sept and the 20:15 UTC deploy
was the next one. It has never been seen green. The rollback printer at the
top of `deploy.sh` reads the same source and has never printed a rollback
point for the same reason.

**The CTO's account of the error, quoted verbatim from the ruling of 13 Sept:**

> On the 13-hour reading — what was run. Amit ran `docker compose … ps web` at
> about 09:20 UTC on 13 September, the morning after. It said `Created 13
> hours ago`. I read "13 hours" as relative to the deploy; it was relative to
> that moment, which puts creation at 20:18 UTC on 12 September — your figure.
> I then attached the wrong cause to a correct measurement (`grep
> '^BUILD_SHA'` returning nothing) and wrote "containers not recreated" into
> the review. Two errors, both mine, both rule 6b.

**#84 had already landed when the "land it alone" ruling in §4 was given.**
`gh pr view 84 --json state,mergedAt` answers `MERGED`, `2026-09-12T18:29:51Z`
(23:59 IST), merge commit `2c264e6`. The ruling was written after that, and the
20:15 UTC deploy shipped #84 together with #91–#99. Its "2/4 red" was never
re-run; the check it added went red for the reason above, not for anything
#84's other changes did.

**Ruling given 13 Sept, on the fix:** the Dockerfile route. `ARG BUILD_SHA` and
`ENV BUILD_SHA=$BUILD_SHA` added to the *runtime* stage so the variable exists
where the check and the printer look; `deploy.sh`'s reading logic untouched;
the `tail -12` on compose output removed (it hid the `Recreate` lines that
would have answered this from the log); the cannot-read branch reworded to say
what it can and cannot conclude. Conditions before merge: the check shown red
on purpose once, the SHA named when it goes green on a real deploy, and the
incident carried in the PR body and in a comment above the two new lines.

**The plaintext `.env` copies and the stray `main` file** named at the end of
the original text: see the developer's report of 13 Sept for what was checked
(live `.env` intact — 39 keys, no placeholders, a superset of both copies) and
what was removed.

---

*Original §3 as written at ~02:00 IST, kept for the record. Every factual claim
below about recreation and about what production serves is wrong; the
measurements quoted in it were correct and the conclusions drawn from them
were not.*

Three attempts to deploy `6f0ded8`, three failures, all different:

1. **Branch guard.** The production checkout was on `feat/csv-import-people`.
   `deploy.sh` refused. Correct. Fixed by hand:
   `git checkout -B main origin/main`. Fourth wrong-branch checkout in a week;
   the guard has stopped every one.
2. **`dial tcp …:22: i/o timeout`.** GitHub's runner could not reach the box
   for 30 s. Transient — the next attempt connected.
3. **`[FAIL] cannot read BUILD_SHA from the running web container`.** The
   deploy ran for four minutes, one step failed, this one. Then measured:
   `docker compose ps web` shows `tatvaos-web-1` **created 13 hours ago**. The
   containers were not replaced.

**What is known:** production serves the previous build; the proof step
correctly refused to call this deploy a success; the new migration
(`20260910-entitlement-overrides.sql`) may or may not have been applied —
**check `\dt core.entitlement_overrides` before assuming either way.**

**Measured, 02:00 IST:** `docker inspect … tatvaos-web-1 | grep '^BUILD_SHA'`
printed **nothing**. The running web container has **no `BUILD_SHA` in its
environment at all** — not empty, absent. So the container serving production
was built by something that never passed the build arg, and the proof step
cannot pass against it no matter what is deployed, until `web` is genuinely
rebuilt and recreated with the arg set.

**What is not known, and is Core's first job:** why tonight's deploy did not
recreate `web`. A changed `ARG BUILD_SHA` → `ENV BUILD_SHA` should produce a
new image layer and force recreation; it did not happen. Either the build did
not run to completion (the log will show), or `up -d` was not reached, or the
compose invocation that built is not the one that checked. The full run log is
saved as `deploy-34716535090.log` in the integration checkout (untracked).
Read it before touching anything.

**Do not "fix" this by adding `--force-recreate` blindly.** Read the comment
block above the check in `deploy.sh` (search `cannot be proven to have
landed`) — it records the incident that made this check exist. Find out why
recreation did not happen, then decide.

Also found on the production checkout, untracked: `infra/docker/.env.backup-…`
and `.env.bak-…` — **plaintext copies of every production secret on disk** —
and the file named `main` that caused the 3 Sept "ambiguous argument"
failure. Core's handover §3 warned about the first shape. Remove both, after
confirming the live `.env` is intact.

## 4. Rulings given today

- **Org-level revoke beats user-level grant.** Failure-mode reasoning, in the
  migration header of `20260910-entitlement-overrides.sql`. UI must flag a
  grant that a revoke will shadow, at the point of granting.
- **#84 (backup runbook, `deploy.sh` rollback source) lands alone**, is
  deployed alone, and a rollback is exercised once before anyone relies on it.
  Its 2/4 red is the apt/Chrome hash-mismatch on the runner image, not the
  branch — needs a re-run first.
- **Recording notice:** re-filed as an unhonoured ruling, not a nicety. Severity
  corrected downward from Connect's report — an on-screen indicator exists.
  Needs Amit's voice; `docs/connect-recording-notice-clip.md`.
- **`20260904-user-recovery.sql` rejected** and out of the tree. Recovery email
  is already shipped in `20260904-user-recovery-email.sql`. Phone-mandatory is
  a product decision for Amit before anyone writes DDL.

## 5. Corrections to things I said, today alone

Recorded because rule 6c is about exactly this and I wrote it.

- Verified rule 6b on a `wip/` branch's working tree and reported it as `main`.
- Repeated "breaks every deploy" about a file that passes CI and fails only in
  production. Four weeks.
- Wrote "recovery email is unstarted" without listing the directory. It was
  shipped, one filename away.
- Told Amit an `x-build` header would answer whether the deploy landed. The
  stamp is inlined into the page, not a header; the `curl` proved only that
  production was up.
- Earlier this week: told Connect to delete `migrations.yml` (Mail caught it);
  cited rule 6b as canonical for three days while it sat unmerged.

Every one was caught by someone reading the actual thing.

## 6. Open, by owner

**Amit**

- Forward Connect's and Core's closing notes if not yet done.
- Rule: is a phone number mandatory for every user?
- Rule: who owns the UI lane (open since 5 Sept).
- Monday 15 Sept, 11:00 IST — Connect capacity test, at a phone.
- Record the recording-notice clip.
- Close #75 (superseded by #94).
- Delete the unlimited `linode-cli-deploy@localhost` access key (from 26 Aug,
  still open per the record).

**Core**

- §3 above, first. *(Done 13 Sept: diagnosis accepted, fix ruled, PR written.)*
- ~~#84 re-run and review; then land alone.~~ Already merged at 18:29 UTC on 12 Sept, before this was written; shipped in the 20:15 UTC deploy. See §3 correction.
- #88 then #89 — nothing in them has compiled; `~/csv-test` build log on the
  box (`grep -E "API_EXIT|WEB_EXIT" /tmp/csvtest-build.log`).
- **CI does not run on stacked PRs** — `ci.yml` filters `pull_request` on the
  *base* branch. With five lanes about to stack, fix before it hides work.
- `ci.yml` bare `apt-get update` (search `apt-get update -qq`) — the Chrome
  repo hash-mismatch flake.
- Entitlements: backfill plan arrays, derived read path, blast-radius count,
  shadowed-grant UI warning, delete `SignupEndpoints.cs:298`.
- The 12 users with `must_change_password = false` from the bulk-import
  window — last known count, re-count first.
- Accept-and-drop gap on the public MX (with Mail) — red first.
- `mail` schema GRANT sweep (with Mail).
- `/srv/tatvaos-production` untracked secrets and the `main` file (§3).

**Mail**

- Prove the bounce pipeline: `BOUNCE_DOMAIN` with RCPT test before/after,
  then keyset, then a real hard-bounce. `docs/onboarding/mail/WELCOME.md` §3.
- `RUNBOOK-2026-08-28.md` step 7 — never run.
- The two #62 checks reported verified without saying what.

**Connect**

- Recording notice fallback: make the swallowed autoplay refusal visible.
- `keep_until_at` documented as pending; it is built. Fix the doc.
- Check `useCaptions` is wired into the room.
- Migration dates `20260901`–`20260910` → real dates.

**Mobile, Hire & People**

- Per their welcomes. No new items from today.

---

*CTO, 13 September 2026, ~02:00 IST. Production up. Nothing lost. Tomorrow is
Claude Code.*
