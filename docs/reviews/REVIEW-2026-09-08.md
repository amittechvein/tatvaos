# Review — 8 September 2026

Window: `main` from yesterday 09:00 to the laptop's last fetch (09:19 today; the VM cannot reach GitHub). Seven PRs merged, 23 commits. One unmerged branch pushed this morning (`fix/verify-migrations-restore`, 3 commits) — reviewed below, not yet cleared.

## What shipped

- Account page → Security: a Recovery card. You can now see, change, resend, and remove your recovery email, and add/change/remove your recovery mobile number. A new number only replaces the old one after a code sent *to the new number* is entered, so a typo can never lock anyone out.
- People page → "Add many": paste rows or upload a CSV, see a preview of what will happen (created / skipped and why), then create. Department per row by name. Temporary passwords shown once with Download CSV. This is the "backend done, UI missing" item from yesterday — closed.
- Contacts: a sender's name in a mail message is now a link to their contact card when they are in the address book. Postal addresses can be added and removed on a contact.
- Bounce handling for the Send API, both halves merged: Postfix routes bounces to the API, the API verifies the signed return address and marks the original send as bounced. **Not yet live** — see send-back 2.
- UI stage 2: shared form controls (Input, Select, Field, Checkbox) and page shells; login, signup, the seven /org pages, account, admin and family pages moved onto them. Muted text across the console was measured at half the required contrast and fixed with one line.

## What I sent back

1. **Core (recovery) — the account-takeover chain I described on 5 Sept is now fully live, and yesterday's PR widened it.** Set recovery email, change phone, remove phone, remove recovery email: none asks for the current password, none emails the primary mailbox, none writes an audit row (`AuthEndpoints.cs` — compare `ChangePasswordAsync`, which does all three). "Reset password via recovery email" (Stage 4) is on `main` — my mistake: on 5 Sept I wrote it was still on a branch; it merged the evening of 4 Sept as PR #33. So today: borrow a signed-in laptop → add your own recovery email → verify → reset the password → owner never told. Fix in one PR, before anything else in this lane: require `currentPassword` on all five mutating recovery endpoints, mail the primary address on every change, `audit.WriteAsync("user.recovery_changed", …)`.
2. **Core (bounce) — merged, not running, and nothing tells the deployer.** The worker reads `Bounce:Domain` and `Bounce:Keys:<id>`; neither appears in `appsettings*.json`, `.env*.example`, or any compose file. With them absent the code fails closed (good) — but that means the Send API still sends plain envelopes and the intake discards everything. Add the two lines to `infra/docker/.env.production.example` with a comment, and a `verify-live.sh` check that the intake listener on 10035 answers. Also `local/postfix/main.cf` says a "guard in Core's domain-creation path" stops a tenant registering the bounce subdomain — no such guard exists (`DomainEndpoints.cs`). Build it or delete the sentence.
3. **Core (verify-migrations branch) — clear only with a calibration run.** The branch restores `verify-migrations.sh` to run psql inside the container. Before it merges: rename one migration so it sorts before its dependency, run the script, show me the red. A restore of a checker with no proof it catches anything is the exact thing it was written to prevent.
4. **Core — `Deploy <deploy@tatvaos.local>` signed three more commits yesterday (recovery card, Add many, contacts).** Third day I have asked. Answer needed: is code being written on the Mumbai server? If it is a wrong `git config` in a worktree, fix it; if it is the server, stop.
5. **Amit — two files in your folder.** `local/postgres/init/20260904-user-recovery.sql` (the broken draft from 5 Sept — still there; it fails on blank phone numbers, and if ever `git add`-ed every deploy stops) and `init_main_export.tgz` (a 114 KB archive in the repo root). In `PS C:\Users\amitd\Downloads\tatvaOS>` run `Remove-Item local\postgres\init\20260904-user-recovery.sql` then `Remove-Item init_main_export.tgz`. Good looks like: `git status` no longer lists either. Note your folder is on branch `feat/ui-stage3-bridge`, not `main` — the deploy sequence starts with `git checkout main`.

## Needs Amit

Nothing new. Branch protection (open since 4 Sept): with the 5 Sept and 8 Sept findings both being "merged before review", the cost of leaving it off is now a live takeover path; the cost of turning it on is that every merge waits for one approval. I recommend on.

## Quiet risks

- The UI lane is being done by Core, on `main`, while the brief says "nobody assigned". The work is good; the question is what Core is *not* doing while it does this (item 1 above, for a start).
- Four Connect migrations still dated 11–12 September. Same as every day; the fix branch is 10 days old.
- Family lane edited `components/mail/MessageView.tsx` and `components/org/AddManyPeople.tsx` — declared in the message, small, fine. Watch it does not become a habit.
- Docs branches (`docs/hr-roadmap`, `docs/space-lane-handover`, `docs/space-virus-scan-brief`) and two `archive/*` branches sit unmerged for 1–4 weeks. Merge or delete by Friday.
