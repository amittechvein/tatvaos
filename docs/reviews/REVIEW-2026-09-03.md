# Review — 3 September 2026

## What shipped

- Mail: IMAP authentication now works for primary passwords. Version 1 broke by stripping '=' from the Argon2 header; v2 surgically removes only the base64 padding at field boundaries, verified on live before commit.
- Mail: Dovecot's auth process no longer dies on every cold start. The local dev stack was missing a mount for the app-password template that production had; CI caught the padding fix before it shipped while running in a broken state.

## What I sent back

Nothing.

## Needs Amit

None.

## Quiet risks

Four Connect migrations are dated 11–12 September, not real dates. The prep branch is queued to fix them. Do not merge Connect features until dates are corrected — running false dates into production breaks rollback and audit trails.

All five developer worktrees are prunable; six branches remain unmerged (two Mail guard-fails for entrypoint, two Space UI honesty, one Docs brief, one Connect date-fix prep). None are stale. Merge when ready.
