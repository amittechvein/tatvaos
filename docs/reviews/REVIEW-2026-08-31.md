# Review — 31 August 2026

## What shipped

- Core's September-dated migration moved to its real date (20260826-core-ai-per-org.sql). Fixes a sort order that would break any fresh database, and aligns C# comment with the actual file name.
- verify-space-link-predicate.sh, a new verification script that catches divergence between the peek and consume public-link predicates. Scans the whole init directory in run order (not just files that sit together), has three positive controls that must fail to prove the script can fail, and refuses to report if any control passes. A textbook rule-6 implementation.
- verify-live.sh improved: IMAP probe now states its calibration clearly (seen once manually, in none of the fifteen test runs) and correct its own caveat about "Waiting for authentication". Dovecot's exact words now in the check.
- Migration comments clarified to identify which definition is live (20260819-space-link-loss-logging.sql) and which is bootstrap (20260816-space-public-links.sql), directing readers to the script rather than manual diffing.

## What I sent back

Nothing. All commits declare their module. Migrations are idempotent (`IF NOT EXISTS`, no destructive updates), properly ordered, and verified twice. The new verify script has explicit calibration and negative controls. Conditions between peek and consume match (the allowed exception—f.id in the WHERE clause—is noted and ruled on).

## Needs Amit

Nothing today.

## Quiet risks

- Five worktrees still prunable (Connect, Core, Mail, Platform, Space). Routine housekeeping when you have time.
- Three future-dated migrations in other lanes (20260911-connect-captions, 20260911-connect-retention-default-30, 20260911-connect-storage-charge, and 20260912-mail-app-passwords-unique). Their owners know; this remains a known-good state until they land.
