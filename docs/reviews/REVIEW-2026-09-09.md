# Review — 9 September 2026

Window: `main` from yesterday 09:54 to the laptop's last successful fetch. Six PRs merged (#59–#66), 21 commits. Eight branches pushed and unmerged, all docs or archive, none new today.

I could reach the code but not the Mumbai server or GitHub from here, so nothing below claims anything about what is *running*. Two things I did run: `apps/web` typechecks clean at `main` (0 errors), and a migration-ordering check that I first proved catches a deliberately mis-ordered file — the schema files still apply in a valid order after yesterday's renames.

## What shipped

- **Admin → Plans can finally grant the products we actually sell.** The page offered People, Payroll, Sheet and Word — all four deleted from the catalogue months ago — and had no checkbox at all for Connect, Calendar or Contacts. That is why you could not give your own organisation Connect. Fixed; the list is now Mail, Contacts, Space, Connect, Calendar.
- **The console is usable on a phone.** Tables stack into readable rows instead of scrolling sideways; long values (email addresses, IDs, domains) now wrap; and the audit table, which was being cut off at the right edge of the screen rather than scrolled, is no longer clipped.
- **The mobile app opens.** The permanent splash-screen hang is fixed, product tiles open and say where they lead, the side rail closes, and the Android project builds. Every network request now writes one diagnostic line — method, path, status, duration, and deliberately never a token, password or email address.
- **Plans and Modal moved off the old YZEN/Bootstrap styling** onto our own design tokens (stage 3 of the UI work).
- **Bounce settings are now passed into the API container.** They were not, which meant the reserved-domain guard we shipped on Monday was switching itself off. Two config readers that treated "blank" as "set" were fixed in the same change.
- **The database now refuses `bounces.tatvaos.com` as a customer domain** — a backstop under the application check, so a fourth code path added next year cannot reopen it.
- **Migrations now have their own CI job:** every schema file is built from an empty database and then applied a second time, on `main` and on any PR that touches them.
- **Handover packs written** for the incoming mobile developer and for a Hire & People hire, one folder per lane under `docs/onboarding/`. I checked every file path and table they cite; all exist.

## What I sent back

1. **Core — the account-takeover path is open for a fifth day, and this is now the only thing in this lane I want worked on.** I checked the code again this morning: `RemoveRecoveryEmailAsync` and the set/change endpoints still ask for no password, send no email to the primary mailbox, and write no audit row — compare `ChangePasswordAsync` twenty lines away, which does all three. Borrow a signed-in laptop, add your own recovery email, reset the password, owner never told. One PR: require `CurrentPassword` on all five mutating recovery endpoints, mail the primary address on every change, write `user.recovery_changed`.
2. **Core — yesterday's bounce send-back is half done.** The compose wiring landed, but `BOUNCE_DOMAIN`, `BOUNCE_KEY_ID` and `BOUNCE_KEY_K1` still appear nowhere in `infra/docker/.env.production.example`. Compose now sets them to empty strings, the code correctly reads empty as off, and so the whole bounce pipeline stays dormant with nothing anywhere telling the deployer why. Add the three lines with a comment.
3. **Core — the Plans fix repeats the mistake it fixes.** The product list is still hard-coded in TypeScript, and `core.products.is_available` is read by nothing in the API or the web app despite existing in the database and in `Entities.cs`. That is exactly how the list drifted for months. Either serve the list from the API or delete the column so nobody trusts it. Related: Connect is labelled "(soon)" in the console although it has been live since 20 August — the flag is cosmetic, but it is now telling admins something false.
4. **Core — calibrate the new migrations CI before we lean on it.** The `MIGRATIONS_DIR` override was added for precisely this and the calibration was not run. Point it at a copy of the folder with one file deliberately renamed to sort before its dependency, and show me the red. I did the equivalent check myself today and proved mine catches it; the CI one has still only ever been seen passing.
5. **Core — fourth day of asking: `Deploy <deploy@tatvaos.local>` signed four more commits yesterday.** Either a worktree has the wrong `git config`, or code is being written on the production server. I need the answer, not the fix.
6. **Me — my own two files are still in your folder, and that is my fault for not clearing them.** `local/postgres/init/20260904-user-recovery.sql` is worse than I said on Monday: it is untracked, so CI and the server never see it, but it *does* run every time that folder starts a local database — and it adds a NOT NULL constraint and a second index on `core.users` that production does not have. Any local testing in that folder has been running against a schema we do not ship. In `PS C:\Users\amitd\Downloads\tatvaOS>`, one line at a time: `Remove-Item local\postgres\init\20260904-user-recovery.sql` then `Remove-Item init_main_export.tgz`. Good looks like `git status` no longer listing either.

## Needs Amit

**Delete YZEN now, or keep patching around it.** Core measured the real cost today: Bootstrap defines `.px-5` with `!important` and loads after Tailwind, so **every Card in the product renders 48px of padding where the design says 20px — on desktop too, not just phones** — and Bootstrap's spacing utilities silently differ from ours everywhere else as well. Yesterday's one-line table fix needed a doubled class name and four `!important`s to win a single argument on a single wrapper.

- **A — finish stage 4: delete YZEN, ~27 files.** Costs roughly a week of one developer, plus a look at every page afterwards. Ends the whole class of problem.
- **B — keep patching.** Costs about an hour each time we notice one, and each patch is another thing to unpick when we eventually do A anyway.

I recommend A, and soon: the collision is quietly wrong on pages nobody has looked at yet, and B makes A more expensive every week. The related decision you have not made is who owns the UI lane — Core has been doing it on top of everything else, which is part of why item 1 above has sat for five days.

## Quiet risks

- `bounces.tatvaos.com` is now written out by hand in three places — Postfix's `main.cf`, `ReservedDomains.cs`, and yesterday's database constraint. Each one says so in a comment. If that subdomain ever moves and one is missed, bounce intake stops without an error anywhere.
- `apps/mobile` sits outside the pnpm workspace (a good decision, well documented) and **no CI workflow builds it**. Nothing will tell us the day it stops compiling.
- Your integration folder is on branch `docs/index-for-new-developers`, not `main`. The deploy sequence starts with `git checkout main`.
- Docs branches `docs/hr-roadmap`, `docs/space-lane-handover`, `docs/space-virus-scan-brief` and the two `archive/*` branches are 1–4 weeks stale. Same line as yesterday; merge or delete.
- `people` was deleted from the product catalogue back in `0028`, so the Hire & People hire arrives to a product code that does not exist. One-line migration when you decide whether it is sold as its own product or inside the suite.
