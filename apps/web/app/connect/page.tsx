import { ComingSoon } from '@/components/ComingSoon';

export const metadata = { title: 'TatvaOS Connect' };

export default function ConnectComingSoon() {
  return (
    <ComingSoon
      product="Connect"
      blurb="Video meetings for your organisation — a link in a calendar
             invitation, joined from the browser, with nothing to install."
    />
  );
}
